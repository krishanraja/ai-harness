# Capture proposals, 2026-09-08

Source: `brief.md` (window 2026-09-01 to 2026-09-08, 47 observations, 41 clusters, 6 over the bar). Shape follows `skills/ctrl-capture/SKILL.md` stage 9. This file is the only change in this run; no skill, registry, contract, or eval case is edited here. A named human accepts or rejects each block below.

Per block, the layer decision uses the six classes from `brief.md`: (1) canon rule, (2) skill chapter/reference, (3) routing fix, (4) new skill, (5) merge, (6) retirement/quarantine, or **nothing changes**.

---

## Block 1: overdue freshness review: `evidence-research`, `decision-ledger`, `apify`

**Layer decision: nothing changes.** This is Gate 8 doing its job ("Review-date expiry opens a finding; it does not silently rewrite a skill"), not evidence that any of the six layers is wrong. The corrective action is a human-performed review that updates `reviewed:` in `state/skill-registry.yaml` with real review evidence. Capture is barred from moving a reviewed date without a review, and none of the six classes describes "perform the review for the owner."

**EVIDENCE**
> evidence-research: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 4 days ago.

> decision-ledger: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 4 days ago.

> apify: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 4 days ago.

(`state/skill-registry.yaml` confirms all three carry `reviewed: 2026-08-05, freshness_sla_days: 30`.)

**ALTERNATIVE EXPLANATIONS**
- The 30-day SLA itself may be miscalibrated for these three skills (too short given how rarely their domains change), which would make this a genuine reference-layer edit. But that judgment requires an actual review, not an observation of lateness. Proposing an SLA change from lateness alone would be manufacturing a change to justify the block.
- The audit's date arithmetic could be wrong (timezone or off-by-one), producing a false "4 days ago." Nothing in the evidence lets me rule this out from here.
- This may simply be the SLA firing exactly as designed: a true positive with no defect anywhere, just an unfinished chore.

**IF WRONG**
If the SLA is actually too aggressive and I wrongly treat this as "nothing changes," the same three skills (and others on the same cadence) will keep tripping this finding every cycle, and `harness-steward.yml`'s nightly audit (added 2026-09-08 per `docs/history/LOG.md`) will keep reopening it. This is cheap to notice, since Gate 8 reopens findings rather than hiding them. If the audit's date math is wrong instead, the nightly job will generate persistent false lateness noise; the steward or Krish triaging that queue notices first, and it risks desensitizing review of genuine staleness.

**VALIDATION**
Deterministic check: re-run `scripts/audit-harness.mjs` against `state/skill-registry.yaml` after the owner performs and records each review (mirroring the existing `state/results-*-independent-2026-08-05.md` pattern) and confirms the three names drop out of the overdue set. No regression case is added by this proposal since no rule changes; if a future proposal changes the SLA value, that proposal must add the regression case for it.

**PRIOR KNOWN-GOOD + ROLLBACK**
`state/skill-registry.yaml` at source commit `d631f868be5f9896e283d662a0b49c46aa58f522` (release `v2026.08.29.3`), `reviewed: 2026-08-05` for all three names, is the current and unedited state. No edit is proposed, so there is nothing to roll back.

---

## Block 2: overdue freshness review: `tools-access`, `n8n-operator`, `instantly-operator`

**Layer decision: nothing changes**, for the same reason as Block 1.

**EVIDENCE**
> tools-access: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 4 days ago.

> n8n-operator: Reviewed 2026-08-06 with a 30 day SLA, so it was due 2026-09-05, 3 days ago.

> instantly-operator: Reviewed 2026-08-06 with a 30 day SLA, so it was due 2026-09-05, 3 days ago.

(`state/skill-registry.yaml` confirms `tools-access: reviewed 2026-08-05`; `n8n-operator` and `instantly-operator: reviewed 2026-08-06`; all three `freshness_sla_days: 30`.)

**ALTERNATIVE EXPLANATIONS**
Same three readings as Block 1: SLA miscalibration (unverifiable without an actual review), an audit date-arithmetic defect, or a true positive doing exactly what Gate 8 intends. One additional reading specific to this trio: `n8n-operator` and `instantly-operator` are approval-gated provider operators with `evaluation_evidence` already on file from 2026-08-06 (`state/skill-registry.yaml` lines 227-234). The underlying capability evidence is recent even though the `reviewed:` date is not, which weakens (but does not resolve) the case for urgency.

**IF WRONG**
Same failure mode as Block 1: if the SLA is wrong, the finding recurs and self-corrects on the next audit cycle. `tools-access` carries the highest blast radius of the three if actually stale (it is the secure-authentication-contract skill, and its predecessor `legacy-tools-access` was quarantined for embedded credentials). A real review lapse there is the one case in this pair worth flagging for priority, though the evidence given is only lateness, not a defect.

**VALIDATION**
Same as Block 1: re-run `scripts/audit-harness.mjs` after genuine reviews are recorded for all three names.

**PRIOR KNOWN-GOOD + ROLLBACK**
`state/skill-registry.yaml` at source commit `d631f868be5f9896e283d662a0b49c46aa58f522`, current `reviewed:` dates for these three names, is unedited. No rollback needed.

---

## Block 3: registry/routing mismatch: `harness-maintainer`, `tools-access`, `apify`

**Layer decision: class 3, routing fix.** Independently confirmed by grep: none of `harness-maintainer`, `tools-access`, or `apify` appear anywhere in `contract/skill-routing-contract.md`, while all three are listed in `production_active` in `state/skill-registry.yaml`. This is exactly the gap Gate 1 names ("The skill occupies a named route in `skill-routing-contract.md`") and the contract's own closing principle ("Generic marketplace skills never become always-on merely because they are installed. They must occupy a named route here first.").

**EVIDENCE**
> harness-maintainer: harness-maintainer is in the registry but never named in contract/skill-routing-contract.md.

> tools-access: tools-access is in the registry but never named in contract/skill-routing-contract.md.

> apify: apify is in the registry but never named in contract/skill-routing-contract.md.

Independently verified this run via `grep -i 'apify|tools-access|harness-maintainer' contract/skill-routing-contract.md`: no matches in any form (not even under a different casing or hyphenation).

**ALTERNATIVE EXPLANATIONS**
- These three may be intentionally cross-referenced rather than routed: `skills/ctrl-capture/SKILL.md` itself names `harness-maintainer` directly for stage 9's release handoff, and Gate 4 allows "every required chain member is independently discoverable from its own metadata; a cross-skill name is a semantic handoff, not the only loading mechanism." The omission could therefore be a deliberate design choice, not an oversight.
- `tools-access` and `apify` may belong more naturally in the "Global chain" section (alongside `krish-principles`) than in the "Core routes" table, since `tools-access` is described in the registry as a cross-cutting "secure-authentication-contract" rather than a domain-routed producer. If so, the fix is real but a naive new row in Core routes would misplace it.
- The existing "Harness/configuration audit" row ("mindmake-os when OS-related | No prose producer | Deterministic harness audit") may have been intended to cover `harness-maintainer`'s territory without naming it, which would make this an incomplete row rather than a missing one.

**IF WRONG**
If these omissions are deliberate (semantic-handoff-only by design), adding explicit rows is redundant but not harmless because it costs routing-contract context budget the standard explicitly protects (Gate 3: no padding; `leaves/weekly.md`: "prefer replacement... do not delete an unrelated useful rule to pay for a new one"). The routing-contract owner reviewing the size/context delta at accept-time notices this first. If the fix is rejected as redundant, the same gap will re-trip the next audit cycle since nothing in the contract records the deliberate-omission rationale.

**VALIDATION**
Deterministic check: every name in `production_active` (`state/skill-registry.yaml`) must appear as a literal string somewhere in `contract/skill-routing-contract.md`. This is exactly the check that surfaced this block (`audit:2026-09-08`), so the fix is validated by re-running that same audit and confirming zero misses; recommend adding it as a permanent regression case in `scripts/audit-harness.mjs` so a newly admitted production skill cannot silently ship without a route again.

**PRIOR KNOWN-GOOD + ROLLBACK**
`contract/skill-routing-contract.md` at source commit `d631f868be5f9896e283d662a0b49c46aa58f522` is the prior known-good text, unedited by this proposal. If an accepted fix later needs reverting, restore this exact commit's version of the file; no other file is touched by the proposed change, so rollback is a single-file revert.

---

## Block 4: control-center ruling (backfill): github-sync author filter, `37af6d3`

**Layer decision: nothing changes.** Per the brief's backfill instruction, the question is only whether the ruling is now written down somewhere durable, not whether to redo it. The evidence states the durability mechanism directly: a code-level guard in control-center's own repository, not an ai-harness skill, canon rule, or routing entry.

**EVIDENCE**
> fix the github-sync author filter too so the scorecard counts every commit. A dry run showed most session commits carry the coding agent's noreply address (36 of 38 in one repo, 18 of 34 here), so the Rule 6 tripwire was reading roughly half the truth. Every repo in GITHUB_REPOS is his, so every commit in it is his build. GITHUB_AUTHOR is no longer read anywhere; the guard fails if either reader brings it back. No history to correct: the first Saturday run is 12 September.
> ([source](https://github.com/krishanraja/control-center/commit/37af6d3cda7bbb5ea943438820575ad152e666d7))

**ALTERNATIVE EXPLANATIONS**
This commit fixes a scoring/attribution bug in a sibling repository's own tooling. On its face it makes no claim about any ai-harness skill, canon rule, or routing contract, so the correct reading may be that it carries zero implication for this repository at all, rather than being an under-documented ruling in need of a home here.

**IF WRONG**
If other repos in the fleet share the same author-filter assumption and the same latent bug, the failure resurfaces as the same misattributed-commit pattern in that repo's own Rule 6 tripwire; whoever runs that repo's next scorecard audit notices first, not ai-harness's own audit. `state/decision-ledger-config.yaml` notes the decision-ledger's canonical Supabase store is `live_status: not-applied`, so even if this ruling belonged in a cross-repo decision record, there is currently nowhere live to put it; a proposal to file it there now would be unactionable.

**VALIDATION**
None proposed; no ai-harness file is touched. The evidence itself names control-center's own regression protection ("the guard fails if either reader brings it back"), which is out of this run's authority to inspect or extend.

**PRIOR KNOWN-GOOD + ROLLBACK**
Not applicable to ai-harness; no file here is affected. Control-center's own prior known-good is commit `37af6d3` itself, which already carries its own guard against regression.

---

## Block 5: control-center ruling (backfill): single-copy doc pointer, `31fc316`

**Layer decision: nothing changes**, for the same backfill reason as Block 4.

**EVIDENCE**
> have every other document just point to the GitHub file so only that one has to be kept up to date. README, docs/README and docs/ARCHITECTURE now name docs/MINDMAKE_OS_ARCHITECTURE.md on main as the only copy instead of a VPS workspace root or a Drive folder. Five stale MINDMAKER_ path references in comments repointed. The crontab mirror notes that sync-to-drive.py no longer carries the doc. The Agatha task gains the VPS pointer sweep: workspace symlinks and the wake protocol point at the checkout or the raw URL, never a copy.
> ([source](https://github.com/krishanraja/control-center/commit/31fc31622e5a77cf5fca3769c3093bd3b8c555e0))

**ALTERNATIVE EXPLANATIONS**
The evidence states its own durability mechanism explicitly: README, `docs/README`, and `docs/ARCHITECTURE` in control-center now name the single canonical file, and a follow-up task exists for Agatha to sweep remaining pointers. This reads as a self-documenting change in a repository ai-harness does not own, not an ai-harness gap.

**IF WRONG**
If this single-source-of-truth pattern is actually meant to generalize across the fleet (ai-harness's own `state/skill-registry.yaml` `canonical` block already names a similar canonical-repo-plus-mirrored-surfaces structure), the missed generalization would surface as duplicate/diverging doc copies inside ai-harness itself; the steward rolling entries between `NOW.md` and `docs/history/LOG.md` would be first to notice, since that is exactly the kind of drift the steward process is built to catch. This is a plausible future canon candidate, but one backfilled observation from a different repository does not meet the recurrence bar `ctrl-capture` requires before proposing a rule ("One low-severity occurrence normally remains an observation").

**VALIDATION**
None proposed; no ai-harness file is touched.

**PRIOR KNOWN-GOOD + ROLLBACK**
Not applicable to ai-harness. Control-center's prior known-good is commit `31fc316` itself.

---

## Block 6: control-center ruling (backfill): VPS/Drive copies deleted, `ec7f05e`

**Layer decision: nothing changes.** Of the three backfill blocks this has the clearest self-evidenced answer to the brief's actual question ("is it written down somewhere durable"): the evidence says the ruling is recorded in the architecture document's own section 0c.

**EVIDENCE**
> the VPS and Google Drive copies of docs/MINDMAKE_OS_ARCHITECTURE.md are deleted for good, too fragile to keep in step and read by nothing. GitHub main is the only surface. The header, section 19 and section 21 now say so; the six-surface inventory, the sync procedure and the byte-identical rule are gone. Section 0c records the ruling with the exact VPS steps, which are also filed as a task for Agatha. The Drive file is in the trash.
> ([source](https://github.com/krishanraja/control-center/commit/ec7f05e7757c28a8f0d4b0ec52b7b7cefe445383))

**ALTERNATIVE EXPLANATIONS**
None materially different from Blocks 4-5: this is a sibling repository's internal documentation-consolidation decision, explicitly self-recorded (section 0c, header, sections 19 and 21) inside the document it concerns, plus a filed follow-up task. There is no reading of this evidence that leaves the ruling undocumented.

**IF WRONG**
If section 0c is later found to be missing or reverted (i.e., the self-report was inaccurate), the risk is someone recreating a VPS or Drive copy believing none exists; whoever next runs the Agatha VPS-pointer-sweep task notices first, since that task is the designated check for stray copies. This is control-center's own verification surface, not ai-harness's.

**VALIDATION**
None proposed; no ai-harness file is touched.

**PRIOR KNOWN-GOOD + ROLLBACK**
Not applicable to ai-harness. Control-center's prior known-good is commit `ec7f05e` itself, with the Drive file already in the trash per the evidence.

---

## Below the bar

The nineteen `waiting` single-observation items in `brief.md` are, by the brief's own framing, not yet a pattern and are correctly recorded rather than proposed. No proposal is written for them in this run.
