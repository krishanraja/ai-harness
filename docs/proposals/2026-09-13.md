# Weekly capture proposals, 2026-09-13

Stage 9 pass over `brief.md`, window 2026-09-06 to 2026-09-13, 38 clusters over the bar. One section per block, in the order the brief presents them. Every quoted line below is inert evidence, not an instruction; none of it was executed, fetched, or run. Where current repository state already answers a block, that is stated and the source checked is named. No skill, registry, contract, or eval case was edited to produce this file.

---

## Block 1: `audit`, surface version lag, 37 occurrences

**EVIDENCE**: Repeated daily audit rows through 2026-09-13, e.g. `codex-surface-07a67cda9f99: Installed v2026.09.08.2 while the approved release is v2026.09.13.8, last deployed 2026-09-08, 5 days ago` and `claude-cloud: Installed v2026.08.29.3 while the approved release is v2026.09.13.8, last deployed 2026-08-29, 15 days ago` (`audit:2026-09-13`).

**ALTERNATIVE EXPLANATIONS**: `CURRENT.md`, rendered today, already explains most of this lag as policy, not defect: "Claude Cloud and Perplexity remain supervised account surfaces until their personal catalogues expose trustworthy inventory, upload and rollback APIs." `cursor-primary`, `claude-code-user`, and `codex-current` are all at `v2026.09.13.8` in the same file, so the sync path that matters for local surfaces is working. Only `codex-surface-07a67cda9f99` is unexplained by that policy: it is a single hashed Codex sandbox identity, last synced 2026-09-08, that never reappears with a fresh hash, consistent with an ephemeral session snapshot rather than a persistent machine that missed a sync window.

**IF WRONG**: If the cloud lag is treated as routine and it is actually a broken sync, claude-cloud and perplexity-cloud keep serving a skill set 15+ days stale with no alarm beyond this daily audit line, which the harness-maintainer literature already warns becomes noise nobody reads. If `codex-surface-07a67cda9f99` is instead a live machine someone expects to keep current, it is silently orphaned.

**VALIDATION**: Before proposing a canon change, resolve the open question rather than guess: ask whether `codex-surface-07a67cda9f99` denotes a durable machine or a one-shot sandbox. If durable, `state/skill-registry.yaml`'s surface list should carry that surface's expected sync owner the same way `harness-sync-lorimer` and `harness-sync-surface` do in `state/heartbeats.json`; a missing declared clock for it would then qualify under the same `EXPECTED_CLOCKS` mechanism already built for block 13. If one-shot, `scripts/audit-harness.mjs` should stop treating a hash with no subsequent heartbeat as an actively monitored surface after some bound, the same way `session-feed` was removed on 2026-09-12 rather than left to age forever.

**PRIOR KNOWN-GOOD + ROLLBACK**: No change proposed yet; this is a question for Krish, not a bounded delta. Nothing to roll back. The cloud-lag half of this finding needs no action beyond what `CURRENT.md` and the handoff proposal already state.

---

## Block 2: `canary`, mis-trigger churn, 31 occurrences

**EVIDENCE**: Spans `v2026.09.08.2` through `v2026.09.13.8`, e.g. `codex-current: instantly-trigger-012: instantly-operator fired on a message that must not trigger it (Repeat-on-mismatch: 1/3 attempts met the declared route...)` recurring at both `v2026.09.13.4` and again at `v2026.09.13.8` (`canary:v2026.09.13.4` and `canary:v2026.09.13.8`, both `codex-current`).

**ALTERNATIVE EXPLANATIONS**: Almost every case named in this block (`brief-trigger-017`, `video-engine-trigger-011/003`, `mindmake-trigger-017`/`os-trigger-009`, `design-intel-trigger-011`, `corpus-trigger-007`, `decision-trigger-020/009`, `n8n-trigger-011`, `principles-trigger-032/013`, `strategy-trigger-032`, `design-trigger-021`, `verification-trigger-032`) is already the subject of an accepted proposal on file: `docs/proposals/2026-09-12-routing-and-canary-repair.md`, `2026-09-12-live-canary-follow-up.md`, `2026-09-13-take-the-brief-exclusion-gate.md`, `2026-09-13-canary-index-and-routing-repair.md`, `2026-09-13-canary-observability-boundary.md`, `2026-09-13-containment-evidence-semantics.md`, and `2026-09-13-representative-canary-follow-up.md`, each approved by a named ruling in this same brief (blocks 29-36). Re-proposing them here would violate the invariant against re-arguing an accepted decision without materially new evidence. The one entry not covered by those files is `instantly-trigger-012` recurring at `v2026.09.13.8`, after `canary-observability-boundary.md` already tried to fix "hypothetical Instantly wording" at `v2026.09.13.4`.

**IF WRONG**: If the recurrence at `.8` is dismissed as already-fixed, `instantly-operator` keeps over-triggering on a message that must not reach an approval-gated provider-mutation skill, understating the containment gap `CURRENT.md` already flags as "one-intermittent-instantly-containment-failure."

**VALIDATION**: The recurrence after a targeted fix is itself the signal for a method question, not a fresh content patch: per `leaves/method.md`'s recognition test, two fixes at the same exclusion and a third recurrence suggests the *stance* of "front-load the exclusion" is not the defect, since it was already applied twice. Route this one item to `harness-maintainer` as a named open case for the next release's live canary sweep, with `instantly-trigger-012` pinned as a required regression before any release is called clean, rather than authoring a third content-only tweak on the same evidence pattern.

**PRIOR KNOWN-GOOD + ROLLBACK**: For the covered cases, prior known-good is each already-named release (`harness-v2026.09.12.3`, `.4`, `harness-v2026.09.12.5`) as recorded in the cited proposals. For `instantly-trigger-012`, no new change is proposed here; it stays an open, tracked case pending a method-level look rather than a fourth patch.

---

## Block 3: `audit`, long descriptions with trailing exclusions, 23 occurrences

**EVIDENCE**: Repeats daily through 2026-09-13 for `krish-design` (1012 chars), `build-apps-with-krish` (989 chars), `ctrl-intake` (941 chars), `krish-voice` (939 chars): "carries negative trigger conditions, on a client that reports shortening descriptions to fit a context budget. Exclusions sit at the end of a description and are what a truncation removes first."

**ALTERNATIVE EXPLANATIONS**: This exact finding was already evaluated and explicitly rejected as a change on 2026-09-12. `docs/proposals/2026-09-12-routing-and-canary-repair.md` states: "The four long descriptions in `krish-design`, `build-apps-with-krish`, `ctrl-intake`, and `krish-voice` are explicitly excluded. A shorter rewrite was already rejected in `state/routing-description-eval-2026-09-12.md`, and PR 42's judge correctly found that a description-budget finding did not prove the removed phrases were non-load-bearing." Checking current file state confirms all four descriptions are unchanged since that ruling.

**IF WRONG**: If the truncation risk is real and growing (e.g. a client update narrows the budget further), the exclusions could silently vanish with no new signal, since this audit line will keep firing at the same volume regardless.

**VALIDATION**: No new validation is proposed; the existing rejection stands. `ctrl-capture`'s invariant is explicit: "Do not re-propose a rejection... without materially new evidence." Nothing in this window's occurrences is new: same four skills, same character counts, same day-over-day repetition already accounted for on 2026-09-12.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable. Nothing changes. If a client-side truncation incident is ever observed in a live canary (an exclusion measurably failing to load), that would be materially new evidence and would reopen this question.

---

## Block 4: `audit`, brain migration debt, 20 occurrences

**EVIDENCE**: Daily through 2026-09-13: `standards_registry: standards_registry holds 122 of 169 rows still marked active that brain/stores.yaml says belongs in the brain, and it has not been moved`; and parallel findings for `canon_documents.krish-canon`, `assumption-ledger`, and `decision-ledger-store`.

**ALTERNATIVE EXPLANATIONS**: `brain/stores.yaml` already carries full first-person diagnosis of each row, including for `decision-ledger-store`: "The skill also says never to create an ad hoc permanent file, so repointing it is a skill rewrite through the proposal path, not a config change," and a `progress_2026_09_09` note showing `brain/decisions.jsonl` already exists and holds fifteen real entries, with the skill simply not yet repointed to it. `docs/harness/OPEN-ON-KRISH.md` separately names the `standards_registry` reconciliation as "the reading job nobody else can do... which is why the audit names it and never performs it." This is documented, judgment-gated backlog awaiting Krish's decision, not an undiscovered skill or routing defect.

**IF WRONG**: If this is left purely as "known and waiting" when it is actually stalled for a fixable reason (e.g. nobody has looked since 2026-08-06), the audit keeps this permanently loud for no new information, which `scripts/audit-harness.mjs`'s own comment warns against: "a permanent finding at a fixed volume is one everybody learns to scroll past."

**VALIDATION**: The `decision-ledger-store` sub-case is the one piece here with an unusually cheap next step: `brain/decisions.jsonl` already exists and is populated, so a `ctrl-build`-routed change to repoint `skills/decision-ledger/references/supabase-storage.md` (confirmed still naming Supabase as canonical) to that file is a bounded, already-scoped skill rewrite, not new discovery. That is a proposal Krish could accept today; it is named here as a candidate, not executed.

**PRIOR KNOWN-GOOD + ROLLBACK**: No skill edit is made in this run. If Krish accepts routing `decision-ledger` to `brain/decisions.jsonl`, prior known-good is the current Supabase-referencing `references/supabase-storage.md`, recoverable from git history, and the change would go through the normal Compile/Build/Check/release path per the routing contract.

---

## Block 5: `audit`, read-only brain clients, 20 occurrences

**EVIDENCE**: Daily through 2026-09-13: `claude-ai-chat: claude-ai-chat reads from the brain and writes nothing back`, and identically for `perplexity-computer`, `n8n-agents`, `routines`.

**ALTERNATIVE EXPLANATIONS**: `state/fleet.yaml` already names each gap and, for the largest one, names the fix: "The biggest reader in the fleet and it has never written one thing back... The import-memory skill already exists and is the cheapest write path available" (`claude-ai-chat`). `perplexity-computer` is recorded as "Same as claude.ai chat, with no import path of its own." `n8n-agents` and `routines` are recorded with their own distinct gaps (a fourth doctrine copy not rendered from canon; two scheduled Routines with no ledger path). This is diagnosed, prioritized backlog, not a missing skill or routing defect inside `ai-harness`'s own contract.

**IF WRONG**: Same risk as block 4: correctly-diagnosed backlog left unflagged as backlog looks identical to an unnoticed gap, and the daily repetition trains toward ignoring it.

**VALIDATION**: None proposed at the skill/contract layer. The one actionable item already named, adopting the existing import-memory skill as `claude-ai-chat`'s write path, is Krish's implementation decision on a different surface, not a change to `ai-harness`'s skills, registry, or routing contract.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; no change proposed.

---

## Block 6: `canary`, void coverage from unreachable positives, 19 occurrences

**EVIDENCE**: E.g. `codex-current: ctrl-compile: no positive canary passed on codex-current (compile-trigger-005 came back wrong-skill, compile-trigger-021 came back wrong-skill)... ctrl-compile is unmeasured` (`v2026.09.12.4`), and similar void reports for `video-engine`, `design-intelligence-search`, and `core` across `v2026.09.08.2` through `v2026.09.13.3`.

**ALTERNATIVE EXPLANATIONS**: This is the same measurement-infrastructure problem already fixed in stages: `docs/proposals/2026-09-13-canary-index-and-routing-repair.md` removed the fictional `core` skill from the canary sheet and required positive canaries for the three real core skills; `2026-09-13-representative-canary-follow-up.md` pinned two representative, reviewed positive canaries per shared core skill specifically to stop void coverage from an unlucky prompt selection. Block 14's coverage trend (28 unmeasured skills on 2026-09-08 down to 7 on 2026-09-13) is direct evidence this fix is working, not evidence of a new gap.

**IF WRONG**: If void coverage is treated as fully solved and a skill quietly regresses to unreachable again, the same blind spot returns without a distinct signal, since a void report and a clean report can look similar in casual reading.

**VALIDATION**: Already specified in the cited proposals: fresh Claude and Codex reports must carry no failed canary verdict and the generated sheet must contain positive cases for every core skill by declared owner, not filename.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in the cited proposals: `harness-v2026.09.13.1` through `.5` form the successive rollback chain, each an immediate rollback target for the next.

---

## Block 7: `canary`, expected-skill wrong-skill misses, 15 occurrences

**EVIDENCE**: E.g. `codex-current: compile-trigger-005: expected ctrl-compile to fire, observed wrong-skill` recurring from `v2026.09.12.4` through `v2026.09.13.7`; `claude-code-user: app-trigger-024: expected build-apps-with-krish to fire, observed wrong-skill` (`v2026.09.13.1`).

**ALTERNATIVE EXPLANATIONS**: Each named case traces to an accepted fix already on file: `app-trigger-024` and `corpus-trigger-007` and `decision-trigger-019` in `2026-09-13-canary-stability-follow-up.md`; `os-trigger-018` and the `mindmake-os`/`content-corpus`/`krish-content-marketer`/`decision-ledger` collision family in `2026-09-13-handoff-and-canary-correction.md`; `uxf-trigger-007` in `2026-09-13-canary-observability-boundary.md`'s UX-foundations sentinel pin; `strategy-trigger-006/025` in `2026-09-13-representative-canary-follow-up.md`'s representative-canary pinning; and `compile-trigger-005` at `v2026.09.13.7` specifically in `2026-09-13-ctrl-compile-import-routing.md`, which found Codex was sending machine-readable-import requests to `ctrl-build` instead of `ctrl-compile` and fixed the discovery-metadata boundary between them. `compile-trigger-021` (`v2026.09.12.4`) is the one case not individually named in a later proposal, though it falls inside the same `ctrl-compile`/`ctrl-build` boundary the import-routing fix targets.

**IF WRONG**: If `compile-trigger-021` is assumed covered by the general `ctrl-compile`/`ctrl-build` boundary fix without being explicitly re-tested, a distinct trigger phrasing could keep missing after the release that claims to have closed this block.

**VALIDATION**: Add `compile-trigger-021` by name to the regression set that `2026-09-13-ctrl-compile-import-routing.md` already commits to adding for the guard-only-versus-activation distinction, so the next release's canary sweep confirms it, not just the case that prompted the fix.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in each cited proposal; no new rollback target is introduced here.

---

## Block 8: `audit`, stale reviewed dates: `evidence-research`, `decision-ledger`, `apify`, 12 occurrences

**EVIDENCE**: `evidence-research: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 7 days ago` (and parallel lines for `decision-ledger`, `apify`), last occurrence 2026-09-11.

**ALTERNATIVE EXPLANATIONS**: `state/skill-registry.yaml` now shows `evidence-research` reviewed `2026-09-12`, `decision-ledger` reviewed `2026-09-13`, and `apify` reviewed `2026-09-12`, all after this block's evidence window closes (last occurrence 2026-09-11). This finding is already resolved by an already-completed review; no occurrence after 2026-09-11 appears in the brief, consistent with the registry update landing on 2026-09-12/13.

**IF WRONG**: If the registry date was bumped without an actual substantive re-read (a "review-date reset" rather than a real review), staleness would be hidden rather than fixed, which Gate 8 explicitly warns against: "Review-date expiry opens a finding; it does not silently rewrite a skill."

**VALIDATION**: No new proposal. If a future audit shows any of these three skills' reviewed date moving again without new evidence of an actual review (e.g. a diff, a session note, or a linked decision), that would be a materially different finding worth its own block.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 9: `audit`, stale reviewed dates: `tools-access`, `n8n-operator`, `instantly-operator`, 12 occurrences

**EVIDENCE**: `tools-access: Reviewed 2026-08-05 with a 30 day SLA, so it was due 2026-09-04, 7 days ago`, and parallel lines for `n8n-operator` and `instantly-operator`, last occurrence 2026-09-11.

**ALTERNATIVE EXPLANATIONS**: Same pattern as block 8: the registry now shows `tools-access` reviewed `2026-09-12`, `n8n-operator` reviewed `2026-09-12`, `instantly-operator` reviewed `2026-09-12`, all after this block's last occurrence.

**IF WRONG**: Same as block 8.

**VALIDATION**: No new proposal; same standing condition as block 8 for reopening.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 10: `canary`, claude-code-user mis-triggers, 11 occurrences

**EVIDENCE**: E.g. `claude-code-user: design-intelligence-search-explicit-001: expected design-intelligence-search to fire, observed not-fired` (`v2026.09.12.4`, recurring `v2026.09.12.5`); `claude-code-user: principles-trigger-032: krish-principles fired on a message that must not trigger it` (`v2026.09.13.3`).

**ALTERNATIVE EXPLANATIONS**: Covered by the same chain as block 2: `2026-09-13-canary-observability-boundary.md` explicitly addresses the `krish-principles` case ("A Codex read of that file is therefore not an implicit routing event and cannot be expected to disappear for social messages... remove it from invocation canaries") and the same logic applies on Claude; `2026-09-13-representative-canary-follow-up.md` and `2026-09-13-take-the-brief-exclusion-gate.md` cover `strategy-trigger-032` and `brief-trigger-017`; `2026-09-13-canary-stability-follow-up.md` covers `corpus-trigger-007`, `decision-trigger-009`, and `design-trigger-021` (via `locked-revision` neighbor handling). `design-intelligence-search-explicit-001` not-fired is the one item that traces to a different cause: the skill was "simultaneously described as routable while hidden from the Codex catalog" per `2026-09-13-handoff-and-canary-correction.md`, addressed there by keeping it "discoverable but retain a strict structured-delegation gate."

**IF WRONG**: If the explicit-invocation not-fired case is treated as fully closed by the discoverability fix alone, an explicit ask for the delegation-gated skill could keep silently failing on Claude specifically.

**VALIDATION**: `design-intelligence-search-explicit-001` should be a named required-pass regression on Claude specifically in the next release sweep, not folded silently into the general discoverability claim.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in the cited proposals.

---

## Block 11: `canary`, codex design-intelligence-search unreachable, 8 occurrences

**EVIDENCE**: `codex-surface-07a67cda9f99: design-intel-trigger-003: expected design-intelligence-search to fire, observed unreachable` (`v2026.09.08.2`); `codex-current: design-intel-trigger-011: expected wrong-skill naming one of krish-design, observed unreachable (Codex target was absent from the catalog.)` (`v2026.09.12.3`, recurring `.4`).

**ALTERNATIVE EXPLANATIONS**: Directly named and fixed in `2026-09-13-handoff-and-canary-correction.md`: "a delegation-gated design skill was simultaneously described as routable while hidden from the Codex catalog," corrected by making `design-intelligence-search` "discoverable but retain a strict structured-delegation gate." The `codex-surface-07a67cda9f99` occurrence is the same stale-surface question raised in block 1: that surface has not re-synced since 2026-09-08, so it cannot show the fix regardless of content correctness.

**IF WRONG**: If `codex-surface-07a67cda9f99` never re-syncs, this specific unreachable finding will look permanently unresolved even after the underlying fix ships, misattributing a surface problem to a routing problem.

**VALIDATION**: Confirm reachability on `codex-current` (the surface that does sync) in the next canary sweep; treat `codex-surface-07a67cda9f99` as out of scope for this specific validation per block 1's open question.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in `2026-09-13-handoff-and-canary-correction.md`.

---

## Block 12: `canary`, unreachable/wrong-skill on core and validators, 8 occurrences

**EVIDENCE**: `codex-current: strategy-trigger-006: expected core to fire, observed unreachable` (`v2026.09.13.2`); `codex-current: check-trigger-021: expected ctrl-check to fire, observed wrong-skill` (`v2026.09.13.4`).

**ALTERNATIVE EXPLANATIONS**: The `core`/`strategy-brief`/`verification-loop` unreachable cases are the same fictional-skill-catalogue defect fixed in `2026-09-13-canary-index-and-routing-repair.md`. `verification-trigger-010/031/008` and `instantly-trigger-004` fall under `2026-09-13-canary-observability-boundary.md`'s exclusion front-loading and adversarial-case reclassification. `uxf-trigger-007` is the same case as block 7. `check-trigger-021` is the one entry in this block with no proposal naming it directly; the closest related evidence is `canary-observability-boundary.md`'s note that "Uncurated suites could still select adversarial adjacent-owner collisions as ordinary positive release sentinels. This produced repeated false release failures for CTRL Check," which is about `ctrl-check` producing false failures as a validator, not about routing *to* `ctrl-check` failing.

**IF WRONG**: If `check-trigger-021` is assumed swept up by the adjacent fix, a genuine `ctrl-check` under-trigger stays open uncounted.

**VALIDATION**: Name `check-trigger-021` explicitly in the next live sweep rather than assuming it is resolved by the CTRL Check false-positive fix, since the two are evidentially distinct claims.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in the cited proposals for the covered items; no new rollback target for `check-trigger-021`, which is flagged for confirmation, not changed.

---

## Block 13: `audit`, expected clocks that never reported, 7 occurrences

**EVIDENCE**: `session-feed: session-feed is an expected clock with no entry in state/heartbeats.json, so it has never run once` (2026-09-08 through 2026-09-11); `harness-sync-lorimer` parallel findings, also through 2026-09-11.

**ALTERNATIVE EXPLANATIONS**: Both are resolved in exactly the way `docs/harness/OPEN-ON-KRISH.md` prescribed. `scripts/audit-harness.mjs` now carries the comment "session-feed was removed from this set on 2026-09-12. It never existed, and..." confirming the "remove it from `EXPECTED_CLOCKS` with a recorded reason" option was taken, which is why no occurrence appears after 2026-09-11 in the brief. `state/heartbeats.json` now shows `harness-sync-lorimer` with a real `last_run` of `2026-09-13T19:09:00.927Z`, confirming Krish ran `Invoke-HarnessSync.ps1` on LORIMER as instructed, which is why block 13 has no entries past 2026-09-11 and block 22 shows it reporting (with a `blocked` status, a separate finding).

**IF WRONG**: If the removal reason for `session-feed` were undocumented, a future reader would see a shrinking expected-clocks list with no explanation. It is documented in the script comment, so this risk is already closed.

**VALIDATION**: None proposed; confirmed resolved by reading current `scripts/audit-harness.mjs` and `state/heartbeats.json`.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 14: `audit`, canary coverage trend, 6 occurrences

**EVIDENCE**: `coverage: 28 of 29 skills have never had a positive canary fire on any surface...` (2026-09-08) improving daily to `coverage: 7 of 29 skills have never had a positive canary fire on any surface, including 3 core skill(s)...` (2026-09-13).

**ALTERNATIVE EXPLANATIONS**: This reads as a live, improving trend line rather than a stuck problem: 28 to 23 to 19 to 7 over the window, tracking the same release sequence (`v2026.09.12.3` through `v2026.09.13.8`) covered by the proposals cited in blocks 2, 6, 7, 10, 11, and 12. An alternative reading, that the drop is an artifact of the recording defect itself (e.g. the fictional `core` skill previously inflating the "never fired" count before `canary-index-and-routing-repair.md` corrected the catalogue), is plausible for part of the improvement and cannot be ruled out from this evidence alone.

**IF WRONG**: If the improvement is mostly a counting artifact rather than real trigger fixes, the residual 7 unmeasured skills (including the 3 named core skills) could be undercounted as a smaller problem than it is.

**VALIDATION**: This is exactly the post-release effectiveness check `leaves/quarterly.md` describes: compare the 3 still-unmeasured core skills' opportunity denominators before declaring the trend real, not just the headline count. Recommended as the next quarterly pass's first item, not a new proposal now.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; this block is a measurement, not a change.

---

## Block 15: `canary`, void core/strategy-brief/verification-loop coverage, 6 occurrences

**EVIDENCE**: `core is unmeasured on claude-code-user: 2 result(s) void because no positive canary passed there` (`v2026.09.13.2`); parallel voids for `strategy-brief` and `verification-loop` at `v2026.09.13.3`.

**ALTERNATIVE EXPLANATIONS**: Same root cause and same fix as blocks 6 and 12: the fictional `core` catalogue entry, corrected in `2026-09-13-canary-index-and-routing-repair.md`, and the representative-canary pinning in `2026-09-13-representative-canary-follow-up.md` that followed it.

**IF WRONG**: Same as block 6.

**VALIDATION**: Same as block 6: fresh reports must show positive cases for all three named core skills, not a fictional `core` entry.

**PRIOR KNOWN-GOOD + ROLLBACK**: As recorded in the cited proposals.

---

## Block 16: `audit`, unreachable skills on `codex-surface-07a67cda9f99`, 5 occurrences

**EVIDENCE**: `video-engine on codex-surface-07a67cda9f99: A canary reported video-engine unreachable... the client could not see the skill at all` (2026-09-08 through 2026-09-11); `design-intelligence-search on codex-surface-07a67cda9f99: 4 canaries reported design-intelligence-search unreachable... at v2026.09.08.2` (2026-09-13).

**ALTERNATIVE EXPLANATIONS**: This is the same stale-surface pattern flagged in blocks 1 and 11: every occurrence in this block names the same surface hash, `codex-surface-07a67cda9f99`, frozen at `v2026.09.08.2` per `CURRENT.md`. It is evidence of that one surface's staleness, not of `video-engine` or `design-intelligence-search` being unreachable on any currently-synced surface.

**IF WRONG**: If this surface is actually a persistent machine someone relies on, five occurrences of "the client could not see the skill at all" is a real, unaddressed outage on it, not a stale-snapshot artifact.

**VALIDATION**: Depends on the same open question as block 1: is `codex-surface-07a67cda9f99` a durable machine or an ephemeral sandbox identity. No proposal is made until that is answered.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; folded into block 1's open question rather than treated separately.

---

## Block 17: `audit`, stale reviewed date: `design-intelligence-search`, 4 occurrences

**EVIDENCE**: `design-intelligence-search: Reviewed 2026-08-07 with a 30 day SLA, so it was due 2026-09-06, 5 days ago` (last occurrence 2026-09-11).

**ALTERNATIVE EXPLANATIONS**: `state/skill-registry.yaml` now shows `design-intelligence-search` reviewed `2026-09-13`, after this block's window closes. Same resolution pattern as blocks 8 and 9.

**IF WRONG**: Same as block 8.

**VALIDATION**: None proposed; same standing condition as block 8 for reopening.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 18: `waiting`, `contentarchives` NOW.md open questions, 3 occurrences

**EVIDENCE**: "whether `Archive\` and `ContentProduction\` move inside `PhotoLibrary\`... whether 'all screenshots should be deleted' overrides the rule that a classifier moves but never deletes... what the remaining categories are" (`contentarchives`, `NOW.md`, 2026-09-08 through 2026-09-11).

**ALTERNATIVE EXPLANATIONS**: This is a different repository's own product/content roadmap question, not evidence about an `ai-harness` skill, route, or contract clause. `ctrl-capture` in this repository proposes changes to this repository's canon.

**IF WRONG**: Not applicable to this proposal file; if this needed harness-level action it would need its own repository's proposal path.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable. Nothing changes here; out of scope.

---

## Block 19: `audit`, unrouted registry entries, 3 occurrences

**EVIDENCE**: `harness-maintainer: harness-maintainer is in the registry but never named in contract/skill-routing-contract.md` (and parallel lines for `tools-access` and `apify`), all dated 2026-09-08.

**ALTERNATIVE EXPLANATIONS**: `contract/skill-routing-contract.md` already names all three in its Core routes table, with an explicit provenance note: "the three rows added 2026-09-09, `harness-maintainer`, `tools-access` and `apify`: **Ruling (Krish, 2026-09-09)**, answering an `unrouted` finding open since 2026-09-08." This block's evidence is entirely from 2026-09-08, the day before the fix landed.

**IF WRONG**: None; this is confirmed resolved by reading the current contract, which the provenance note itself points to as the answer.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 20: `audit`, missing evaluation evidence, 3 occurrences

**EVIDENCE**: `evaluation coverage: 27 of 29 skills have no evaluation evidence anywhere: no evaluation_evidence row in state/skill-registry.yaml and no measurement in state/evals/. 19 per-skill result files sit in state/ as loose markdown from 2026-08-05 and were never folded in` (2026-09-08, three variants of the same finding as the audit's own count logic was refined that day).

**ALTERNATIVE EXPLANATIONS**: Checking `state/skill-registry.yaml` today confirms this is still substantively true: only one `evaluation_evidence` row exists across the whole registry. This is not stale evidence; it is an open, unresolved gap that just did not recur as a distinct daily line after 2026-09-08 because the audit apparently counts it once per its own cadence, not because it was fixed.

**IF WRONG**: If treated as a one-time historical note because it stopped recurring daily, the underlying gap, most skills lacking any recorded proof they were ever behaviorally tested, stays invisible day to day even though Gate 6 requires held-out behavior suites and Gate 9 requires structural/trigger/behavior tests to pass "from a clean commit."

**VALIDATION**: Propose a rule addition to Gate 9 in `contract/active-skill-quality-standard.md`: a release is not eligible to mark a skill's status `production` unless `state/skill-registry.yaml` carries a non-empty `evaluation_evidence` reference for it, so the 19 loose markdown result files either get folded into the registry as real rows or the skills they cover are visibly not `production`-eligible until they do. This is a rule-in-canon-block layer change (class 1), the cheapest sufficient layer, since the gap is that the standard does not yet make evaluation evidence a release-blocking fact, not that any one skill mis-triggers.

**PRIOR KNOWN-GOOD + ROLLBACK**: Prior known-good is the current Gate 9 text in `contract/active-skill-quality-standard.md`, unedited in this run. Rollback, if the rule proves too strict (e.g. blocking a release over a skill that is genuinely evidence-light but low-risk), is reverting the added clause; no skill statuses are changed by this proposal itself.

---

## Block 21: `audit`, em dash violations outside the validator's exemptions, 3 occurrences

**EVIDENCE**: `29 lines in 12 files: The canon carries 29 em dashes outside the files the validator refuses on: skills/apify/references/actors.md, skills/ctrl-build/leaves/writing.md, skills/ctrl-capture/leaves/method.md, skills/ctrl-capture/leaves/quarterly.md, skills/ctrl-check/SKILL.md, skills/ctrl-compile/SKILL.md, skills/ctrl-compile/leaves/profile.md, skills/ctrl-compile/reference/ctrl-import.md, skills/de[...]` (2026-09-09 through 2026-09-11).

**ALTERNATIVE EXPLANATIONS**: A current search for the em-dash character (`—`, U+2014) across `skills/` and `contract/` finds it only in vendored third-party CSV/data files under `skills/design-intelligence-search/scripts/vendor/`, none of the 12 files this block names. `scripts/validate-surfaces.mjs` independently asserts "no em dash in the canon, the templates, the rendered adapters or the... brain files" as a passing check. This finding has no occurrence after 2026-09-11 in the brief, consistent with a fix landing between then and now.

**IF WRONG**: If the fix only touched a subset and this grep is missing an encoding variant (e.g. em dash inside a code fence the validator skips), a residual violation could exist uncounted. This check used a plain-text grep, not the validator's own logic.

**VALIDATION**: Run `scripts/validate-surfaces.mjs` itself (not reproduced here) as the authoritative check before treating this as fully closed; this proposal's own grep is a corroborating signal, not a replacement for it.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes based on available evidence.

---

## Block 22: `audit`, blocked machine sync runs, 3 occurrences

**EVIDENCE**: `harness-sync-lorimer: harness-sync-lorimer last reported a blocked machine run at 2026-09-13T19:09:00.927Z`; `harness-sync-surface: harness-sync-surface last reported a blocked machine run at 2026-09-13T07:31:32.023Z`.

**ALTERNATIVE EXPLANATIONS**: `scripts/audit-harness.mjs` already treats this correctly, not as a false pass: "Read the machine evidence pull request. A current clock proves the task ran; it does not turn a refused install or failed canary into health." A `blocked` status from `Invoke-HarnessSync.ps1` is documented as the noop/blocked path the script takes when e.g. a canary fails, which is exactly the ongoing canary remediation covered in blocks 2, 6, 7, 10, 11, 12, and 15. This is a symptom of the same live behavioral gaps, not a distinct sync-mechanism defect, and directly explains block 1's observation that `codex-surface-07a67cda9f99` and the cloud surfaces are not advancing: a blocked run does not promote a new release to a surface.

**IF WRONG**: If `blocked` is instead masking an unrelated script defect (not a real behavioral failure), the sync would stay stuck even after the canaries genuinely turn green, and this line would keep firing for the wrong reason.

**VALIDATION**: Once the open canary items in blocks 2, 7, and 12 are confirmed clean in a live sweep, the next `harness-sync-lorimer`/`harness-sync-surface` run should report `ok`. If it still reports `blocked` after a clean canary sweep, that would be new evidence of a distinct sync defect worth its own proposal.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; no change proposed, tied to the outcome of already-tracked canary work.

---

## Block 23: `ruling`, standing approval for `record_harness_observation`, two occurrences, one a direct correction

**EVIDENCE**: `The narrow redacted record_harness_observation write is standing-approved without per-event confirmation; no other outbound write or payload expansion is approved` (commit `840ad04b6845`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: This is already durably written into `contract/krish-operating-contract.md`: "`record_harness_observation` is a standing, narrow exception approved by Krish for the redacted payload defined in Observation capture. It authorizes no other outbound write and no expansion of the payload." Reading the current file confirms the ruling is recorded, not just stated in a commit message.

**IF WRONG**: If the contract wording were ever narrower than the actual ruling (e.g. missing the "no expansion of the payload" boundary), a future session could over-read its own authority. Comparing the two, the contract text matches the ruling's scope.

**VALIDATION**: None proposed; confirmed durable by reading `contract/krish-operating-contract.md` directly.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 24: `ruling`, GitHub-sync author filter fix (backfill)

**EVIDENCE**: `control-center` commit `37af6d3cda7b`: "fix the github-sync author filter too so the scorecard counts every commit... GITHUB_AUTHOR is no longer read anywhere; the guard fails if either reader brings it back. No history to correct: the first Saturday run is 12 September."

**ALTERNATIVE EXPLANATIONS**: This is a `control-center` repository change, outside `ai-harness`'s own canon. The brief's own framing for backfill blocks is explicit: "the useful question here is whether the ruling is now written down somewhere durable, not whether the change was made." The commit message itself, in `control-center`'s own history, is that durable record for that repository.

**IF WRONG**: Not applicable to this repository's proposal file.

**VALIDATION**: Not applicable; no `ai-harness` action follows from this.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope for `ai-harness`'s canon.

---

## Block 25: `ruling`, documentation single-source-of-truth (backfill)

**EVIDENCE**: `control-center` commit `31fc31622e5a`: "have every other document just point to the GitHub file so only that one has to be kept up to date... Five stale MINDMAKER_ path references in comments repointed."

**ALTERNATIVE EXPLANATIONS**: Same as block 24: a `control-center` ruling, durably recorded in that repository's own commit history.

**IF WRONG**: Not applicable to this repository.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope.

---

## Block 26: `ruling`, retiring the VPS/Drive architecture-doc copies (backfill)

**EVIDENCE**: `control-center` commit `ec7f05e7757c`: "the VPS and Google Drive copies of docs/MINDMAKE_OS_ARCHITECTURE.md are deleted for good... GitHub main is the only surface."

**ALTERNATIVE EXPLANATIONS**: Same as block 24: durably recorded in `control-center`'s own history and canon sections cited in the evidence itself (header, section 19, section 21, section 0c).

**IF WRONG**: Not applicable to this repository.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope.

---

## Block 27: `ruling`, "push to main by default" (contentarchives)

**EVIDENCE**: `contentarchives` commit `a5022221ba34`: "'push to main by default.' Recorded in Standing constraints rather than left in a conversation, because the next session will not have the conversation."

**ALTERNATIVE EXPLANATIONS**: A different repository's own standing-constraints record, already self-described as durably recorded by the commit itself.

**IF WRONG**: Not applicable to this repository.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope.

---

## Block 28: `correction`, idempotent retry must return the original receipt even past a daily write limit

**EVIDENCE**: `[codex] An identical stable retry is not a new write and must return the original receipt even when the daily write limit has already been reached. Related: Observation capture idempotency.` (`inbox:mcp:c6f55d3f070bdf72d1f9fc736620de4b552112e1`, 2026-09-11).

**ALTERNATIVE EXPLANATIONS**: `contract/krish-operating-contract.md`'s Observation capture section already requires "a stable retry key so a retry is idempotent," but does not explicitly state the limit-collision case this correction names: that an identical retry must succeed with the original receipt even after the daily write limit is otherwise exhausted. The correction is narrower and more specific than the current wording, so this reads as a real, still-open gap in the contract text rather than something already covered by the general idempotency sentence. The actual enforcement of a daily write limit and idempotent-retry behavior lives in the `record_harness_observation` tool's implementation, which is outside this repository; nothing here can verify the code-level behavior, only the doctrine that should govern it.

**IF WRONG**: If the general idempotency sentence is treated as already sufficient, an implementer could reasonably read "idempotent" as "same key produces the same write outcome" without realizing a stable-key retry must bypass a rate limit that would otherwise reject it, producing a false negative for a caller that assumed its evidence was recorded.

**VALIDATION**: Propose adding one sentence to `contract/krish-operating-contract.md`'s Observation capture section, immediately after the existing idempotency clause: an identical stable-key retry returns the original receipt regardless of any daily or rate limit reached since the original write. This is a rule-in-canon-block layer change (class 1). Validation for acceptance would be confirming the actual tool's behavior against this wording once accessible, which is outside this repository's test surface; short of that, the change is doctrine clarification only.

**PRIOR KNOWN-GOOD + ROLLBACK**: Prior known-good is the current Observation capture section, unedited in this run. Rollback is reverting the added sentence if the tool's real behavior turns out to already satisfy the intent without needing it spelled out.

---

## Block 29: `ruling`, v2026.09.12.4 live evaluation sweep authorization

**EVIDENCE**: `The v2026.09.12.4 live evaluation sweep was explicitly requested and approved; every candidate citation now carries that provenance directly` (commit `e7689c6a2615`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: Already durably recorded: `brain/stores.yaml`'s `citation-coverage-ledger` entry states "Rows admitted from v2026.09.12.4 onward name the asker who authorised the sweep; earlier rows are retained as legacy history and make no asker-provenance claim." The ruling is written into the store's own schema documentation, not left only in the commit message.

**IF WRONG**: Not applicable; confirmed durable.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 30: `ruling`, v2026.09.12.4 rollout retention of failed results

**EVIDENCE**: `The v2026.09.12.4 rollout and live canary sweep were explicitly requested and approved in this interactive session; retain their failed results as evidence` (commit `009ea6a45b7e`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: The behavior this ruling authorizes, retaining failed canary reports rather than erasing them, is already a standing invariant independent of this specific ruling: `ctrl-capture`'s own change-and-history invariants state "Never erase superseded clauses or proposal history in place," and every cited 2026-09-12/13 proposal explicitly preserves prior failed reports ("No historical canary report is rewritten"). This ruling reaffirms, rather than introduces, existing doctrine.

**IF WRONG**: Not applicable; the underlying behavior is already governed independently of this single ruling.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 31: `ruling`, citation-coverage ledger as bounded maintenance

**EVIDENCE**: `The requested citation-coverage ledger is bounded maintenance of the existing harness, serves the keep-him-honest job, and is shown before completion in public PR 42` (commit `55290bc23083`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: Already durably recorded in `docs/proposals/2026-09-12-routing-and-canary-repair.md`'s "Rule 6 disposition" section, which restates this exact reasoning ("serves the existing `keep him honest` job... is not being built privately: public PR 42 shows the implementation, evidence, review failures, and corrections before release") as part of the accepted, merged proposal record.

**IF WRONG**: Not applicable; confirmed durable in the merged proposal file.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 32: `ruling`, approval of the named harness gap-closure batch

**EVIDENCE**: `Approve the named harness gap-closure batch, including the punctuation-only canon revisions, preserved catalogue move, quarantine containment, credential rotation and scrub, progressive release, and supervised cloud uploads, subject to the recorded verification gates` (commit `e1953773a436`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: This is a broad batch approval whose individual items (punctuation revisions, catalogue move, quarantine, credential rotation, progressive release, cloud uploads) are the kind of durable facts that belong in per-item records rather than one paraphrased summary. The commit itself is the durable record of the approval; whether every named sub-item individually completed and was separately verified is not established by this evidence alone.

**IF WRONG**: If one sub-item (e.g. credential rotation) was approved here but never separately completed and verified, a summary ruling like this could create the impression of closure without item-level proof. `docs/harness/OPEN-ON-KRISH.md` and `state/credential-exposure-2026-09-12.md` (referenced from `CURRENT.md` as "open, approved remediation in progress") suggest credential rotation specifically remains open rather than closed by this ruling alone.

**VALIDATION**: No new proposal; the sub-items should each be tracked to their own completion evidence (as `CURRENT.md` already does for credential exposure) rather than treated as closed by this single batch-approval line.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes here beyond noting that batch approval is not the same as batch completion.

---

## Block 33: `ruling`, `record_harness_observation` authorization

**EVIDENCE**: `Authorize record_harness_observation as a narrow redacted external write, with ctrl-capture remaining the only interpretation and promotion owner` (commit `840ad04b6845`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: Same evidence source as block 23; already durably recorded in `contract/krish-operating-contract.md` and reinforced in `contract/ctrl-capture`... specifically `skills/ctrl-capture/SKILL.md`'s own "Read-only by default; exact proposal/decision-store write if any" capture contract field, which matches "ctrl-capture remaining the only interpretation and promotion owner."

**IF WRONG**: Not applicable; confirmed durable.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 34: `ruling`, personally commissioned infrastructure exemption

**EVIDENCE**: `This personally commissioned harness infrastructure is explicitly asked for and is not subject to the paying-leader product-demand test` (commit `840ad04b6845`, 2026-09-12).

**ALTERNATIVE EXPLANATIONS**: This is a scope-of-authority ruling about how to classify a specific request, not a rule with an obvious canon home to check for durability the way block 23's tool-authorization language has. No search of `contract/` or `skills/` in this pass turned up an existing durable restatement of "paying-leader product-demand test" as a named test with an exemption clause; it may exist only in conversation history and this commit.

**IF WRONG**: If this test and its exemption criteria are real, recurring judgment tools Krish expects the system to apply consistently, and they exist nowhere durable, a future session could misapply the test to similar personally commissioned work without knowing the exemption exists.

**VALIDATION**: This is a genuine open question rather than a proposal: is "the paying-leader product-demand test" a standing doctrine that belongs in `contract/krish-principles` or `krish-operating-contract.md`, or a one-off framing specific to this request? Recommend asking Krish directly rather than guessing; no bounded change is proposed until that is known.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; no change made.

---

## Block 35: `ruling`, proceed with the audited correction plan

**EVIDENCE**: `proceed with the audited handoff, canary, routing, release, and rollout correction plan` (commit `f7a400206d25`, 2026-09-13).

**ALTERNATIVE EXPLANATIONS**: Already durably recorded as the status line and "User authority" note in `docs/proposals/2026-09-13-handoff-and-canary-correction.md` ("Status: approved for implementation, verification, merge, release and governed rollout by Krish Raja's instruction, 'ok go'...").

**IF WRONG**: Not applicable; confirmed durable in the merged proposal file.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 36: `ruling`, approval of PR 45 and v2026.09.12.5 publication

**EVIDENCE**: `Approve PR 45 for merge, immutable publication of harness-v2026.09.12.5, and governed rollout. Credential rotation remains separately gated` (commit `0e3423d7ca71`, 2026-09-13).

**ALTERNATIVE EXPLANATIONS**: Already durably recorded in `docs/proposals/2026-09-12-live-canary-follow-up.md`'s status and action-time-approval lines ("'yes, merge all to main and close this out'... Credential rotation remains outside this approval") and reflected in `CURRENT.md`'s "Credential exposure lifecycle: open, approved remediation in progress" line, which correctly keeps rotation separately gated as this ruling requires.

**IF WRONG**: Not applicable; confirmed durable and consistently reflected in current state.

**VALIDATION**: None proposed.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; nothing changes.

---

## Block 37: `ruling`, independently governed station harnesses (mindmake-video-studio / content-engine)

**EVIDENCE**: `production machinery must use independently governed station harnesses, not one accumulating Markdown file` (`content-engine` commit `20bf14753692`, 2026-09-13).

**ALTERNATIVE EXPLANATIONS**: A different repository's own architecture ruling, out of scope for `ai-harness`'s canon.

**IF WRONG**: Not applicable to this repository.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope.

---

## Block 38: `ruling`, video references live outside Git (mindmake-video-studio / content-engine)

**EVIDENCE**: `Video references are inspiration for videos produced by the Content Engine and must live outside Git in the governed Drive library` (`content-engine` commit `54beea4195c9`, 2026-09-13).

**ALTERNATIVE EXPLANATIONS**: A different repository's own content-storage ruling, out of scope for `ai-harness`'s canon.

**IF WRONG**: Not applicable to this repository.

**VALIDATION**: Not applicable.

**PRIOR KNOWN-GOOD + ROLLBACK**: Not applicable; out of scope.

---

## Below the bar

The 20 `waiting` observations recorded below the bar in `brief.md` are single occurrences, not yet a pattern, per the brief's own framing. None is proposed on here; manufacturing a change from a single observation would violate `ctrl-capture`'s recurrence-qualification policy. They remain visible, unpromoted observations.

## Summary of proposed layer changes

Of 38 blocks: 24 resolve to **nothing changes** (already fixed, already rejected, already durably recorded, or out of this repository's scope); 8 are **already covered by an accepted, on-file proposal** and are named rather than re-proposed (blocks 2, 6, 7, 10, 11, 12, 15, and part of 22); 2 are **open questions for Krish** rather than proposals (blocks 1/16's stale-surface identity, and block 34's undocumented "paying-leader product-demand test"); 1 is a **candidate skill-rewrite** named but not executed (block 4's `decision-ledger` repointing); and 2 are **real class-1 canon-rule proposals** made here: block 20's evaluation-evidence release gate, and block 28's idempotent-retry-past-limit clarification.

Named human acceptance is required for the two rule proposals (blocks 20 and 28) and the `decision-ledger` repointing candidate (block 4) before any of them proceeds through Compile/Build/Check/release. Nothing in this file has been applied.
