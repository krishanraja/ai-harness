# Independent design-skill evaluation - 2026-08-05

## Scope

Evaluated `krish-design` with Claude Code 2.1.222 in safe mode using fresh executor and judge contexts, disabled tools and session persistence, and a USD 1 hard cap per Claude call. Raw outputs remain under ignored `work/`; this report contains redacted outcomes only.

The executor received the complete candidate skill and blind cases but not the behavior rubric. A fresh judge scored the executor output afterward.

## Final outcome

- Trigger routing: 21/21 pass.
- Behavior: 23/23 pass after full and focused correction reruns.
- Hard authority/security failures: 0.
- Corrected and adjacent final set: 8/8 pass with no findings.

## Located failures and corrections

The first run confirmed the existing taste and interaction doctrine but found contract omissions in stage ownership, state/range proof, accessibility, failure classification, stale-render recovery, private evidence, external tool boundaries, and handoff completion.

Corrections made:

- added a design preflight for state of use, user/action, governing rule, data truth, materiality, current brand/voice, authority, and proof;
- routed routine locked-system repairs through `krish-build` without reopening taste;
- required rejected-mock root-cause classification before revision;
- added real code/data feasibility and randomized tiebreaker order to divergent concept selection;
- required rendered sparse fallbacks, long/translated content, narrow/wide viewports, fresh self-contained high-resolution renders, and accessibility range checks;
- added private-evidence redaction/recapture with reproducible context and an honest unverified fallback;
- made ethical interaction and the business trade-off explicit for dark-pattern requests;
- required exact external Canva file/account/artboard approval, a safe local draft, and post-return rendered verification;
- made the design/build/orchestrator/UX/voice handoffs explicit and required combined rendered proof after handoff;
- required capture exports to preserve complete context and verbatim reasoning while excluding unanswered defaults;
- required approved revisions to enter the orchestrator's canonical state artifact and read-only UX audits to return reproducible evidence.

Every failed case and adjacent collision case passed after correction. No aggregate score hid a failure and no authority gate was lowered.

## Residual uncertainty

This proves reviewed synthetic behavior, not Krish's personal reaction to a real artifact. Production admission still requires a real rendered-design canary, unanchored Krish feedback, approved revision readback, implementation handoff, responsive/accessibility proof, rollback evidence, and cross-client discovery/parity.
