interface:
  display_name: "Video Engine"
  short_description: "Launch the Mindmake video production engine"
  default_prompt: "Use $video-engine to open the latest GitHub-authoritative Mindmake Video Engine and recommend the strongest next action."
policy:
  allow_implicit_invocation: true
